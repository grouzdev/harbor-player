{
  "name": "harbor-player-website",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "astro dev --host 127.0.0.1",
    "build": "astro build",
    "preview": "astro preview --host 127.0.0.1",
    "check": "astro check && node --test tests/release.test.ts",
    "test": "playwright test",
    "mockups": "node scripts/mockups.mjs"
  },
  "engines": { "node": ">=24.0.0" },
  "dependencies": { "astro": "7.3.4" },
  "devDependencies": {
    "@astrojs/check": "0.9.10",
    "@playwright/test": "^1.58.0",
    "typescript": "^5.9.3"
  }
}
